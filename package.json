{
  "name": "relix",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "web": "turbo dev --filter @relix/web --",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "react-native-web": "~0.19.6"
  }
}
